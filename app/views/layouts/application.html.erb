<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title(yield :title)%></title>
    <meta name="description" content="<%= title(yield :description)%>">

    <meta property="og:locale" content="pt_BR" />
    <meta property="og:type" content="site" />
    <meta property="og:title" content="<%= title(yield :title)%>" />
    <meta property="og:description" content="<%= title(yield :description)%>" />
    <meta property="og:image" content="<%= title(yield :image)%>"/>
    <meta property="og:url" content="<%= request.original_url %>" />

    <link href='http://fonts.googleapis.com/css?family=Lato:400,300,700' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Annie+Use+Your+Telescope' rel='stylesheet' type='text/css'>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>

  </head>
  <body>

    <div class="modal" id="cmodal" tabindex="-1" role="dialog" aria-labelledby="ModalSouCarioca" aria-hidden="true"><div class="modal-dialog"><div class="modal-content"></div></div></div>

    <header>
      <%= render 'layouts/navigation' %>
    </header>

    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>

    <footer>

      <div class="ft-links">
        <a href="/">Home</a>
        <a href="/page/about">Sobre</a>
        <a href="/user/search">Cariocas</a>
        <span>SouCarioca Versão 0.1</span>
      </div>

      <p>Nós <i class="fa fa-heart"></i> Cariocas</p>

      <small>Se você não nasceu no Rio, mas se identifica com a cultura Carioca você é um Carioca de alma e coração.</small>

    </footer>

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-42101961-1', 'auto');
      ga('send', 'pageview');

    </script>
  </body>
</html>
